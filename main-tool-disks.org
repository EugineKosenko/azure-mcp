#+title: Інструмент ~disks~

Повертає керовані диски: ім'я, група, SKU, розмір у ГБ, стан і VM, до якої диск прив'язано. Виклик — ~GET
.../providers/Microsoft.Compute/disks~ (~api-version=2023-04-02~, перевірено через ~az --debug~).
Непривʼязаний диск позначено прямо: він залишається тарифікованим, тож це витік витрат, як і статична
публічна IP без власника (див. [[file:main-tool-pubips.org][~pubips~]]). Опис для ~tools/list~ і диспетчеризація — в [[file:main-tools.org][~main-tools.org~]].

* Модуль

#+begin_src rust :noweb yes :mkdirp yes :tangle src/tools/disks.rs
  <<Декларації>>

  <<Визначення>>

  pub async fn run(http: &reqwest::Client, arguments: &serde_json::Value) -> serde_json::Value {
      <<Дії>>
  }

  #[cfg(test)]
  mod tests {
      use super::*;

      <<Тести>>
  }
#+end_src

* Таблиця

~managedBy~ — ідентифікатор VM, до якої диск прив'язано (верхній рівень відповіді, а не ~properties~); у
непривʼязаного диска його немає, тоді ~vm=-~. ~diskState~ приймає значення ~Attached~, ~Unattached~,
~Reserved~ та інші; позначку додаємо лише для ~Unattached~. ~Reserved~ у живому виводі мають диски
вимкнених (~deallocated~) VM: диск прив'язано, а машина не працює, і за нього все одно платять. Групу
виводимо малими літерами, як у [[file:main-tool-vms.org][~vms~]]. Фільтр ~name~ не залежить від регістру.

#+begin_src rust :noweb-ref Декларації
  use crate::client;
  use crate::tools;
#+end_src

#+begin_src rust :noweb-ref Визначення
  fn line(disk: &serde_json::Value) -> String {
      let state = tools::text(disk, "/properties/diskState");

      format!(
          "{}  {}  {}  {} ГБ  {}  vm={}{}",
          tools::text(disk, "/name"),
          tools::part(tools::text(disk, "/id"), "resourceGroups").to_lowercase(),
          tools::text(disk, "/sku/name"),
          tools::shown(disk, "/properties/diskSizeGB"),
          state,
          tools::last(tools::text(disk, "/managedBy")),
          if state == "Unattached" { "  (не прив'язаний)" } else { "" },
      )
  }

  fn table(disks: &[serde_json::Value], name: Option<&str>) -> String {
      let lines: Vec<String> = tools::sorted(disks)
          .into_iter()
          .filter(|disk| name.is_none_or(|name| tools::same(tools::text(disk, "/name"), name)))
          .map(line)
          .collect();

      if lines.is_empty() { "Дисків не знайдено.".to_string() } else { lines.join("\n") }
  }
#+end_src

* Дія

#+begin_src rust :noweb-ref Дії
  let scope = match tools::scope(arguments).await {
      Ok(scope) => scope,
      Err(message) => return tools::reply(Err(message)),
  };
  let path = format!("{}/providers/Microsoft.Compute/disks?api-version=2023-04-02", scope);

  tools::reply(client::list(http, &path).await.map(|disks| table(&disks, arguments["name"].as_str())))
#+end_src

* Перевірки

Диск ОС прив'язаний до VM і непривʼязаний диск без ~managedBy~.

#+begin_src rust :noweb-ref Тести
  fn disks() -> Vec<serde_json::Value> {
      vec![
          serde_json::json!({
              "name": "win-server_osdisk_1",
              "id": "/subscriptions/s/resourceGroups/TRIAL/providers/Microsoft.Compute/disks/win-server_osdisk_1",
              "sku": { "name": "Premium_LRS" },
              "managedBy": "/subscriptions/s/resourceGroups/TRIAL/providers/Microsoft.Compute/virtualMachines/win-server",
              "properties": { "diskSizeGB": 127, "diskState": "Attached" },
          }),
          serde_json::json!({
              "name": "old-data",
              "id": "/subscriptions/s/resourceGroups/demo-rg/providers/Microsoft.Compute/disks/old-data",
              "sku": { "name": "Standard_LRS" },
              "properties": { "diskSizeGB": 32, "diskState": "Unattached" },
          }),
      ]
  }

  #[test]
  fn table_marks_unattached() {
      assert_eq!(
          table(&disks(), None),
          "old-data  demo-rg  Standard_LRS  32 ГБ  Unattached  vm=-  (не прив'язаний)\n\
           win-server_osdisk_1  trial  Premium_LRS  127 ГБ  Attached  vm=win-server"
      );
  }

  #[test]
  fn table_filters_by_name() {
      assert!(table(&disks(), Some("OLD-DATA")).starts_with("old-data"));
      assert_eq!(table(&disks(), Some("other")), "Дисків не знайдено.");
  }
#+end_src
